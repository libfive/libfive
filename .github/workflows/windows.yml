name: Windows-Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: windows-latest 

    steps:
    - uses: actions/checkout@v2

#  Windows doesn't work yet.  Blocking on: 
#  - https://github.com/microsoft/vcpkg/issues/12573
