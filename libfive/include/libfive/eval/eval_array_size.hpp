// This definition lives in its own header so we can include it
// in both eval_array.hpp and oracle.hpp, since having either
// include the other causes horrible include loops.
#define LIBFIVE_EVAL_ARRAY_SIZE 256
